# dap-plugin-api

Loads and validates adapter plugin YAML. Lives in `dap-core` because it is shared runtime infrastructure — not part of the manifest-only `dap-plugins` project.

Manifest files: [`../../../dap-plugins/builtin`](../../../dap-plugins/builtin/) (or `$DAP_PLUGINS_DIR`).
