# dap-core

Session management, routing, and proxy orchestration.

```toml
dap-core = { path = "../dap-core" }
```

Depends on [`instance-manager`](../instance-manager/), [`dap-protocol`](../dap-protocol/), and [`dap-plugin-api`](../dap-plugin-api/).
