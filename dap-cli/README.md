# dap-cli

Control plane CLI for the DAP proxy: session discovery, debug commands, gdb-style REPL, and MCP server.

```bash
dap-cli session list
dap-cli plugin list
dap-cli debug start --program ./main.py
dap-cli debug repl --adapter fake main.py
```
